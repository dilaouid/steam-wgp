import styled from "styled-components";
import { Container } from "react-bootstrap";

export const StyledLogoSeparator = styled.img`
    z-index: 0;
    position: absolute;
    margin-left: 46.5%;
    margin-top: -4%;
    width: 7%;
`;

export const StyledContainer = styled(Container)`
    font-family: 'Archivo Narrow', sans-serif;
`;